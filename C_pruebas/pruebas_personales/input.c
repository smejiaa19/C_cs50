#include <stdio.h>

int main(){
    int entrada;
    printf("Ingrese un valor: ");
    scanf("%d", &entrada);
    // Lo que debemos hacer para usar el metodo scanf, es primero poner un 
    // Identificador relacionado al tipo de dato que queremos
    // Luego escribir coma y un and seguido del nombre de la variable
    
}   